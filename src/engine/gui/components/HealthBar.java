package engine.gui.components;

 /*

import engine.entity.Entity;
import engine.entity.component.SpriteComponent;
import engine.entity.component.TransformComponent;
import engine.graphics.texture.Texture;
import engine.maths.Vec4;

public class HealthBar extends ProgressBar{

    Entity health;

    /*

    public HealthBar(float x, float y, float width, float height, Texture bgTexture, Texture barTexture) {
        //super(x,y,width,height,bgTexture,barTexture);
        health = new Entity("health bar");
        health.addComponent(new TransformComponent());
        health.addComponent(new SpriteComponent(new Vec4(1.0f, 1.0f, 1.0f, 1.0f), barTexture, worldWidth, worldHeight));
        //entities.add(health);
    }

} */
